PORT=3001
API_PORT=3001
ANTHROPIC_API_KEY=
# Set this to enable the login prompt (required once deployed publicly).
# Any username works; only the password is checked.
APP_PASSWORD=
# Optional: override where the JSON data file lives (e.g. a mounted volume in production)
DATA_DIR=
